package com.moon.nugasam.constant

class PrefConstants {
    companion object {
        const val KEY_ME = "key"
        const val KEY_NAME = "name"

        const val KEY_ROOM = "key_room"

        const val AD_ORDER = "ad_order"
    }
}